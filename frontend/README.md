# Frontend

Static dashboard starter for the MVP application feed, kill switch, autonomy threshold, and daily cap.

Open `index.html` directly in a browser for the current static version. Replace the local fixture data in `src/app.js` with API calls once the backend endpoints exist.
